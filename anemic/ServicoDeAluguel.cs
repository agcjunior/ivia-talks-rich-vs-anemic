namespace anemic
{
    public class ServicoDeAluguel
    {
        public void Alugar(int filmeId, int clienteId)
        {
            // verifica se filmeId existe no BD
            // verifica se clienteId existe no BD
            // Cria um objeto Aluguel
            // Salva no BD
        }
    }
}